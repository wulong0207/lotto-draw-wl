package com.hhly.drawcore.remote.${package}.service;

import com.hhly.drawcore.remote.${type}.service.IDraw${type?cap_first}Service;

/**
 * @desc ${name}
 * @author huangchengfang1219
 * @date ${(.now)?string('yyyy年MM月dd日')}
 * @company 益彩网络科技公司
 * @version 1.0
 */
public interface IDraw${className}Service extends IDraw${type?cap_first}Service{
	
}
